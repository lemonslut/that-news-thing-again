# Secrets for Kamal deployment
# Reads from local files and environment variables
# DO NOT commit actual secret values to git!

# Container registry password (GitHub Personal Access Token for ghcr.io)
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails master key for credentials (production-specific)
RAILS_MASTER_KEY=$(cat config/credentials/production.key)

# Postgres container password (must match database.password in credentials)
POSTGRES_PASSWORD=$POSTGRES_PASSWORD

# Grafana admin password
GF_SECURITY_ADMIN_PASSWORD=$GF_SECURITY_ADMIN_PASSWORD
