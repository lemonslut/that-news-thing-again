# Secrets for Kamal deployment
# Pull these from environment variables or a secrets manager
# DO NOT commit actual values to git!

# Container registry password (GitHub Personal Access Token for ghcr.io)
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails master key for credentials
RAILS_MASTER_KEY=$(cat config/master.key)

# Database password (shared between app and postgres accessory)
DATABASE_PASSWORD=$DATABASE_PASSWORD
POSTGRES_PASSWORD=$DATABASE_PASSWORD

# API keys
NEWS_API_KEY=$NEWS_API_KEY
OPENROUTER_API_KEY=$OPENROUTER_API_KEY

# Sidekiq Web UI password
SIDEKIQ_WEB_PASSWORD=$SIDEKIQ_WEB_PASSWORD
